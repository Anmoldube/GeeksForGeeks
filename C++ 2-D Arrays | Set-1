vector<vector<int>> transpose(int a[][M], int n)
{
    vector <vector<int>> ans;
    for(int i=0;i<n;i++){
        vector <int> temp;
        for(int j=0;j<n;j++){
            int ele = a[j][i];
            temp.push_back(ele);
        }
        ans.push_back(temp);
    }
    return ans;
}
